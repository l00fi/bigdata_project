apiVersion: 1

providers:
  - name: 'Default'
    orgId: 1
    folder: 'General'           # Папка в меню Grafana
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10   # Как часто проверять изменения в файле (удобно для разработки)
    options:
      path: /var/lib/grafana/dashboards